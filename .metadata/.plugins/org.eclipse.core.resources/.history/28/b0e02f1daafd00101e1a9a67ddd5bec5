package com.revplay.test;

import static org.mockito.Mockito.*;
import static org.junit.Assert.*;

import org.junit.Before;
import org.junit.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import com.revplay.dao.UserDao;
import com.revplay.model.User;
import com.revplay.service.UserService;

public class UserServiceTest {

    @Mock
    private UserDao userDaoImpl;

    private UserService userService;

    @Before
    public void setUp() {
        MockitoAnnotations.initMocks(this);
        userService = new UserService(userDaoImpl);
    }

    // TEST REGISTER
    @Test
    public void testRegisterSuccess() {
        when(userDaoImpl.registerUser(any(User.class))).thenReturn(true);

        boolean result = userService.register("harish", "harish@gmail.com", "123456", "admin");

        assertTrue(result);
        verify(userDaoImpl).registerUser(any(User.class));
    }

    // TEST LOGIN
    @Test
    public void testLoginSuccess() {
        User mockUser = new User();
        mockUser.setUsername("harish");

        when(userDaoImpl.login("harish", "123456")).thenReturn(mockUser);

        User result = userService.login("harish", "123456");

        assertNotNull(result);
        assertEquals("harish", result.getUsername());
        verify(userDaoImpl).login("harish", "123456");
    }

    // TEST VERIFY USER
    @Test
    public void testVerifyUser() {
        when(userDaoImpl.verifyUser("mail@test.com", "harish")).thenReturn(true);

        boolean result = userService.verifyUser("mail@test.com", "harish");

        assertTrue(result);
        verify(userDaoImpl).verifyUser("mail@test.com", "harish");
    }

    // TEST RESET PASSWORD
    @Test
    public void testResetPassword() {
        when(userDaoImpl.updatePassword("mail@test.com", "newpass")).thenReturn(true);

        boolean result = userService.resetPassword("mail@test.com", "newpass");

        assertTrue(result);
        verify(userDaoImpl).updatePassword("mail@test.com", "newpass");
    }
}
